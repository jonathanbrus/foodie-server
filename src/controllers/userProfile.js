const users = require("../models/user");

const updateProfile = (req, res, nex) => {};

const addAddress = (req, res, nex) => {};

const changePassword = (req, res, nex) => {};

module.exports = {
  updateProfile: updateProfile,
  addAddress: addAddress,
  changePassword: changePassword,
};
