const products = require("../../../models/products");
const foods = require("../../../models/food");

const reviewProduct = (req, res, nex) => {};

const reviewFood = (req, res, nex) => {};

module.exports = {
  reviewProduct: reviewProduct,
  reviewFood: reviewFood,
};
